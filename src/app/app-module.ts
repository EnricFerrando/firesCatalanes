import { NgModule } from '@angular/core';

// This file is kept for reference only.
// The application uses standalone components defined in app.config.ts
// and app.ts instead of NgModule-based configuration.

@NgModule({
  providers: []
})
export class AppModule { }
